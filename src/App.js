import React from 'react'
import { SafeAreaView, ScrollView, Text } from 'react-native'

export default () => (
  <SafeAreaView>
    <ScrollView>
      <Text>Hello World</Text>
    </ScrollView>
  </SafeAreaView>
)
